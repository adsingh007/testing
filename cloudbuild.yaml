steps:
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', '$PROJECT_ID/$REPO_NAME/nodejs-app:$BRANCH_NAME-$BUILD_ID', './nodejs-app/']
- name: 'gcr.io/cloud-builders/docker'
  args: ['push', '$PROJECT_ID/$REPO_NAME/nodejs-app:$BRANCH_NAME-$BUILD_ID']

images:
- '$PROJECT_ID/$REPO_NAME/nodejs-app:$BRANCH_NAME-$BUILD_ID'

logsBucket: 'gs://cloud_b_logs/'

timeout: 600s

options:
  logging: GCS_ONLY
